Install
-------

Install homebrew:

    open https://brew.sh

Install the dotfiles:

    git clone git@github.com:twelvelabs/dotfiles.git
    cd dotfiles
    brew bundle install
    rcup -x "README.md"
